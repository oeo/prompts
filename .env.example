# Optional: Specific GPG key to use for encryption/decryption
# Format: last 16 characters of your key ID, or email, or name
WARD_GPG_KEY=

# Optional: Additional GPG recipients (comma-separated) who can decrypt files
# Can use either email or last 16 characters of key ID for each recipient
# If not set, encrypts only for WARD_GPG_KEY or default key
WARD_GPG_RECIPIENTS=

# Optional: Custom private folder path (relative to project root)
# Default: private
WARD_PRIVATE_FOLDER=

# Optional: Custom archive folder path (relative to project root)
# Default: .archives
WARD_ARCHIVE_FOLDER=
